import 'package:flutter/material.dart';

class My404 extends StatelessWidget {
  const My404({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('没有数据!!!'),
    );
  }
}
